import * as React from 'react';

const heart = {
  backgroundImage: 'url(src/components/Games/Savannah/gameScreen/components/healthBar/heart/heart.svg)'
}

const Heart = () => (
  <div className="heart" />
);

export default Heart;
