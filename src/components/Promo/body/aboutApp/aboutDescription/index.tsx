import * as React from 'react';
import './index.scss';
import DescriptionTabsContainer from './gamesAbout';

const AppDescription: React.FC = () => (
  <div>
    <DescriptionTabsContainer />
  </div>
);

export default AppDescription;
